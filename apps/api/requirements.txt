fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13
pydantic>=2.7
pydantic-settings>=2.3
openai>=1.35
redis>=5.0
pgvector>=0.3
tenacity>=8.3
arq>=0.26
httpx>=0.27
mcp[cli]>=1.6

# Editable install of the sibling memory package: apps/api's MCP client spawns
# `python -m mcp_memory.server` as a subprocess, so it must be importable here too.
-e ../../packages/mcp-memory
